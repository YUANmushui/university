import {
  StyleSheet
} from 'react-native';

export let cateStyle = StyleSheet.create({

  barTitle: {
    fontSize: 16,
    marginLeft: 16,
    marginTop: 15,
  },

  container: {
    flex: 1
  },
});