import {
  StyleSheet
} from 'react-native';

export let toolBarStyle = StyleSheet.create({

  back: {
    marginTop: 40,
    marginLeft: 15
  },

  title: {
    flex: 1,
    fontSize: 20,
    color: '#666',
    paddingLeft: 15
  },

  container: {
    height: 50,
    alignItems: 'center',
    flexDirection: 'row',
    backgroundColor: '#fff'
  }
})